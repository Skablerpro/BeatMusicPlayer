<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BeatMusic" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryBackground</item>
        <item name="colorPrimaryVariant">@color/primaryBackground</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/colorMidBlue</item>
        <item name="colorSecondaryVariant">@color/primaryHighlight</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="tabText" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">11sp</item>
    </style>
    <style name="scrollText" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">18sp</item>
    </style>

    <style name="SplashScreen" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/colorLightBlue</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/colorLightBlue</item>
        <item name="android:navigationBarColor" tools:targetApi="l">@color/colorLightBlue</item>

    </style>
</resources>